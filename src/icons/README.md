Drop custom `.svg` files here to use them as `<Icon name="filename" />`.

Icons from the Boxicons (`bx:`), Bootstrap Icons (`bi:`), and Remix Icon
(`ri:`) sets are pulled from the installed `@iconify-json/*` packages and do
not need files in this directory.
